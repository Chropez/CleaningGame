{{#paper-list}}
  {{#each players as |player|}}
    {{#paper-item}}
       <img src={{player.user.avatar}} alt={{player.user.name}} class="md-avatar">
      <div class="md-list-item-text">
        {{player.user.name}}
      </div>
    {{/paper-item}}
  {{/each}}
{{/paper-list}}
